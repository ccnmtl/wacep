import {
    moduleFor,
    test
} from 'ember-qunit';

moduleFor('controller:games-modal', 'GamesModalController', {
});

// Replace this with your real tests.
/*test('it exists', function() {
    var controller = this.subject();
    var store = controller.get('store');
    //ok(controller);
});*/
