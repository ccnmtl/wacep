import {
    moduleFor,
    test
} from 'ember-qunit';

moduleFor('view:purchase-input', 'PurchaseInputView');

// Replace this with your real tests.
test('it exists', function() {
    var view = this.subject();
    ok(view);
});
